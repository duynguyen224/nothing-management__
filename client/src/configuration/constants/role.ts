export interface IRole {
    ADMIN: string;
    YARD_OWNER: string;
    CLIENT: string;
}

export const ROLES: IRole = {
    ADMIN: 'admin',
    YARD_OWNER: 'yard-owner',
    CLIENT: 'client'
};
